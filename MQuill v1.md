下面给你一版**取长补短后的最佳、合理新设计文档**。
我先给结论：**最优方案不是把 MQuill 定义成“世界模拟系统”，也不是只做“UI 很强的写作器”，而是做成“以工作台为产品外壳、以结构化工件为中台、以检索/记忆/模拟为后端内核”的小说创作操作系统。** 旧稿最强的是产品判断：MQuill 必须是“创作引擎 + 编辑工作台”，而不是聊天壳；正式版最强的是工程骨架：多 Agent、分层检索、GroundingPack、ConsistencyJudge、可交付接口。两者合并后，方向才真正稳。   

---

# MQuill v1 统一设计文档

## 基于优秀工作台 UI、结构化工件、多 Agent 协作与分层检索的通用小说创作引擎

## 1. 产品定义

`MQuill` 不应被定义为“自动写小说的模型壳”，而应定义为：

**一个面向小说创作全流程的创作操作系统。**

它的前台是专业工作台，支持项目总览、创建新书、章节导航、场景管理、正文编辑、修订审阅、版本对比与问题跟踪；它的中台是结构化工件系统，维护项目、世界、角色、剧情、场景、版本、评审、记忆；它的后台是由多 Agent、分层检索、记忆更新和一致性校验组成的创作引擎。旧稿已经明确 MQuill 的正确定位是“创作引擎 + 编辑工作台”，而正式版则进一步把后台定义成“世界模拟 + 自进化写作 + 检索基座”，这两者并不冲突，应该合并。   

一句话定义：

**MQuill 是一个以工作台 UI 承载创作流程、以结构化工件承载小说状态、以 Agent 编排和检索记忆承载生成能力的通用小说创作系统。**

---

## 2. 对现有两版方案的评估

### 2.1 旧稿的优点

旧稿最大的优点，是产品判断非常准。它明确指出 MQuill 要解决的不是“能不能写一章”，而是如何稳定地产出完整、一致、可修订、可长期维护的小说；同时它反复强调作者控制权、编辑系统、版本系统、工件系统和中篇优先，这些判断非常成熟。尤其“不要把 UI 做成聊天壳”“优先建设工件系统、编辑系统和版本系统”“人工修改优先、锁定内容不可覆盖”这几个原则，应该保留为顶层约束。  

### 2.2 旧稿的不足

旧稿偏产品与概念层，虽然方向正确，但工程抓手还不够具体。比如 Agent 接口、检索分层、GroundingPack、场景模拟与一致性校验的执行链路还不够落地。它告诉我们“应该这样做”，但没有把系统拆到足够接近开发。 

### 2.3 正式版的优点

正式版最大的优点，是把后台工业化了。它给出了从 IdeaAgent 到 EvolutionAgent 的清晰链路，也把检索明确提升为基础设施，拆出了外部事实检索、内部 Lore 检索、叙事记忆检索和风格约束检索，还定义了多组 JSON Schema 和 API。这个部分对长篇一致性、历史/现实题材真实性、世界设定稳定性非常重要。  

### 2.4 正式版的不足

正式版的问题在于“后台太强，前台太弱”。它把 MQuill 推向了“工业级叙事模拟引擎”，但对真正决定产品可用性的工作台交互、作者控制权、修订决策、SceneCard 作为一级工件的页面流转、版本/问题/修订联动，还不够重视。它提到 UI 工作台规范，但粒度仍偏概述，离你上传 HTML 所体现的真实产品形态还有距离。 

### 2.5 综合判断

最合理的新方案应当保留旧稿的产品原则，并吸收正式版的工程基础设施。
也就是说：

* **产品上**：以工作台为中心，而不是以 Agent 为中心
* **架构上**：以结构化工件为中枢，而不是直接让 Agent 互相传自然语言
* **生成上**：以 Scene 为生产粒度，而不是整章一次性黑盒生成
* **可靠性上**：以检索 + 记忆 + 一致性校验为基座，而不是单纯靠 prompt
* **协作上**：以“作者可控制、可锁定、可回滚、可比较”为最高原则。  

---

## 3. 新版顶层设计原则

### 3.1 工作台优先

MQuill 首先是一个创作工作台，页面和交互不是展示层，而是产品能力本身。上传的 HTML 已经体现了这一点：它有总览页、创建新书、自动生成进度、编辑页、场景编辑、修订模态、版本比较、评估问题联动和 UI 状态持久化。新版设计必须沿这个方向继续，而不是退回“单页聊天”。   

### 3.2 工件优先

系统内部不应让 Agent 直接以自由文本互相传递上下文，而应统一写入工件：Project、WorldBible、Character、Plot、Chapter、SceneCard、Draft、RevisionTask、VersionRecord、EvaluationIssue、Memory、Skill。这样才能做长期维护、精准修订和局部更新。旧稿和正式版都支持这一方向，应正式定为中台原则。 

### 3.3 Scene 优先

章节不是最小生产单元，**Scene 才是最小生产与修订单元**。HTML 中场景卡已经具备编辑、重生成、状态管理和批量生成正文的能力，这说明 SceneCard 应成为系统一级实体。 

### 3.4 检索优先

检索不是外挂功能，而是生成前置条件。正式版明确提出“检索不是增强，是基础设施”，这是对的，但要进一步落地到每个页面动作和每次生成任务里。 

### 3.5 作者优先

人工修改优先于 Agent 输出；锁定内容不可覆盖；影响 canon 的修改必须触发回写和影响范围提醒。这些应写入硬规则，而不是只是“交互建议”。 

---

## 4. 新版总体架构

我建议把 MQuill v1 定义成五层架构。

### 4.1 Experience Layer

负责产品页面和所有用户可见操作。包括：

* 作品总览页
* 创建新书
* 自动生成进度页
* 三栏创作工作台
* Scene 编辑模态
* 正文修订模态
* 版本比较页
* 评估/问题面板

这一层要直接对齐现有 HTML，而不是重新想象。HTML 已经验证了总览、场景编辑、版本比较、修订和评估联动的基本可行性。   

### 4.2 Workflow Layer

负责把创作过程变成显式流程：

`项目创建 → 创意 → 世界 → 角色 → 剧情 → Scene 规划 → Scene 模拟 → 文本渲染 → 评审 → 一致性校验 → 修订 → 版本记录 → 记忆更新`

正式版的 Agent 流水线给出了很好的后台骨架，而 HTML 的自动生成进度和修订流程给出了前台流程节点。 

### 4.3 Artifact Layer

负责所有结构化工件的读写、版本化和引用关系。
这是系统中台，负责把“小说状态”结构化存下来。正式版的数据模型已经提供了骨架，旧稿提供了更丰富的创作实体类型。 

### 4.4 Intelligence Layer

负责多 Agent 编排、策略路由、Skill 选择、生成参数控制。
这一层继承正式版的 Agent 体系，但需要重新组织，让它服务工作台，而不是让工作台服从 Agent。

### 4.5 Grounding Layer

负责分层检索、记忆更新和一致性校验，包括：

* 外部事实检索
* Lore 检索
* Narrative Memory 检索
* Style 检索
* GroundingPack 组装
* ConsistencyJudge

这是保证长篇、历史、现实、复杂世界观题材可靠性的核心基础设施。 

---

## 5. 页面与信息架构

### 5.1 我的作品

作为系统首页，展示项目列表、状态、字数、更新时间与入口。
这一页要承担“资产管理”和“创作回流”，而不是只是 marketing 封面。HTML 已经提供成熟方向。

### 5.2 创建新书

保留现有字段：书名、书籍类型、语言、分类标签、内容描述、封面色调、AI 自动模式；新增字段建议：篇幅目标、读者类型、叙事视角、创作模式。前者来自 HTML，后者来自旧稿对篇幅/题材/风格/受众解耦的要求。 

### 5.3 创作工作台

维持三栏结构：

* 左栏：项目导航、卷章树、标签和状态
* 中栏：正文编辑与版本主视图
* 右栏：SceneCard / AI 场景助手 / 局部建议

右栏必须继续支持收起展开和状态记忆，因为这体现了用户写作姿势的个性化。HTML 已经做了 scene panel collapsed state persistence。

### 5.4 Scene 编辑

Scene 编辑应作为一级模态而不是小弹窗附属，因为它承担剧情粒度控制。HTML 已具备 Scene 编辑、描述改写、AI 重生成、状态更新。 

### 5.5 修订工作台

修订流程统一采用四段式输入：

* 修改建议
* 修订目标
* 保持不变
* 应用方式（替换 / 附加 / 分支）

HTML 中 `saveRevision` 已经具备 replace、append、branch 的分支逻辑，这非常好，必须升格为正式设计。

### 5.6 版本与比较

版本不是后台自动备份，而是作者决策工具。
HTML 中已存在版本记录、比较器、分快照、关联评估问题的逻辑，说明“版本—问题—修订”三者必须打通。 

### 5.7 评估问题台

评估结果不应只是总分，而应以问题卡形式进入修订流，支持“从问题跳修订”“问题绑定版本”“问题状态跟踪”。HTML 代码已经显示问题与修订版本是可关联的。

---

## 6. 核心工件模型

推荐正式采用以下核心工件。

### 6.1 Project

作品项目主实体，包含名称、类型、语言、标签、描述、模式、状态。

### 6.2 ProjectCharter

定义主题、目标读者、风格边界、禁忌、创作目标。

### 6.3 WorldBible

保存地理、经济、规则、势力、时间基线与 canon。

### 6.4 Character

角色基本信息、目标、秘密、关系、语言风格、行为约束。

### 6.5 PlotArc / Chapter / SceneCard

剧情层级结构。
其中 SceneCard 是重点，包含标题、摘要、目标、冲突、视角、状态、排序、来源、生成建议、关联正文片段。Scene 在正式版和 HTML 中都已有雏形，但需要从简化的 `Scene{text,version,state}` 升级为真正可编辑的计划工件。 

### 6.6 DraftSegment / DraftChapter

正文实体，支持局部替换与分段定位。

### 6.7 RevisionTask

正式字段建议：

* target_scope
* suggestion
* goals
* constraints
* linked_issue_id
* apply_mode
* candidate_text
* accepted_text

这个设计直接吸收了旧稿的“批注解释层”和 HTML 中修订工作流。 

### 6.8 VersionRecord

建议字段：

* id
* label
* source
* summary
* parent_id
* branch_name
* linked_issue_ids
* snapshot_content
* created_at

这与 HTML 当前逻辑高度一致。

### 6.9 EvaluationIssue

建议字段：

* id
* type
* severity
* title
* reason
* location
* suggestion
* status
* linked_version_id

### 6.10 Memory

沿用旧稿与正式版，合并为五层：

* Canonical Memory
* World State Memory
* Narrative Memory
* Style Memory
* User Intent Memory

旧稿中的 Working Memory 可并入运行时上下文，不一定需要长期持久化。 

---

## 7. 新版 Agent 架构

新版 Agent 不应只按“谁更酷”命名，而应按工作台任务组织。

### 7.1 BootstrapAgent

根据创建新书信息生成 `ProjectCharter` 初稿。
这是旧稿和 HTML 合起来自然产生的新角色。

### 7.2 IdeaAgent

负责主题、核心冲突、题材和创意方向。

### 7.3 WorldArchitect

负责世界设定，但必须调用外部事实与世界模板检索。

### 7.4 CharacterEngine

负责角色档案与行为约束。

### 7.5 PlotPlanner

负责长程剧情或分卷结构。

### 7.6 ScenePlanner

把剧情分解为 SceneCard 列表，并维护排序与状态。
这是最应该从旧稿里补到正式版里的 Agent。

### 7.7 SceneSimulator

只做因果推演，不直接写散文。这个设计来自正式版，非常值得保留。

### 7.8 WriterAgent

只把 scene state 渲染成文本，不擅自改剧情逻辑。

### 7.9 CriticEditor

负责初轮评审、修改建议和风格调整。

### 7.10 ConsistencyJudge

负责事实、世界规则、角色、时间线和信息一致性。

### 7.11 RepairAgent

根据问题单和 GroundingPack 进行定向修复。
这比让 CriticEditor 直接“批评后顺手改”更稳。

### 7.12 MemoryUpdater

把确认后的修改、设定和事件写回工件与记忆。

### 7.13 EvolutionAgent

从成功修订和高分案例中总结技能，但应继续保留“先人工骨架，再逐步自动进化”的门禁原则。 

---

## 8. 检索与 Grounding 设计

这一块保留正式版核心思想，但改写成更适合产品落地的版本。

### 8.1 四类检索源

* **事实检索**：历史、地理、制度、职业、技术
* **Lore 检索**：世界规则、势力、术语、角色事实
* **Narrative 检索**：前文事件、伏笔、关系、秘密
* **Style 检索**：项目风格约束、禁忌、节奏、视角

正式版提出的四大检索基座是正确的，建议原样保留。

### 8.2 GroundingPack

每次生成 Scene 或执行修订前，都应生成 GroundingPack，最少包含：

* facts
* lore
* memory
* style
* constraints
* current_task

这比只把长上下文直接扔给模型稳定得多。正式版已有 GroundingPack 概念，应升级为正式运行时对象。

### 8.3 题材化检索强度

正式版提出历史/现实最高强度、科幻逻辑优先、奇幻 Lore 优先、实验文学风格优先，这个分题材检索策略很有价值，应保留。

---

## 9. 创作工作流

### 9.1 全自动模式

`Bootstrap → Idea → World → Character → Plot → Scene → Simulate → Write → Critique → Judge → Repair → Save`

适合快速生成提案、中短篇初稿和世界骨架。旧稿和正式版都支持全自动模式。 

### 9.2 共创模式

`作者确认关键节点 → 系统补全与生成 → 作者选 Scene → 局部修订 → 问题回流 → 版本决策`

这是 MQuill 默认推荐模式，也是最适合工作台产品的主模式。 

### 9.3 作者主导模式

作者手写剧情点、场景或正文，系统只提供 Scene 建议、评审、修订、一致性校验和风格统一。
这与旧稿中“作者主写 + agent 辅助”的定位一致。

---

## 10. 版本、评估与修订闭环

这个闭环是新版最重要的落地机制：

`Draft → EvaluationIssue → RevisionTask → Candidate → VersionRecord → Accept/Reject → MemoryUpdate`

这比“写完就改，改完就覆盖”的方式强得多。
HTML 当前已经具备这个闭环的雏形：评估问题可绑定修订，修订可生成候选稿，候选稿可创建新版本，版本又可回写问题状态。新版必须把这套逻辑从 demo 变成正式架构。

---

## 11. v1 / v2 / v3 路线图

### 11.1 v1：中篇工作台 MVP

目标是跑通“项目创建—Scene—正文—修订—版本—问题”的完整闭环。
优先级应继续遵循旧稿建议：先做中篇，不急着长篇宇宙。

### 11.2 v2：长篇稳定化

加入更强的 Lore 检索、时间线校验、角色漂移检测、卷纲与伏笔管理。
这一阶段吸收正式版的世界模拟和检索基础设施。

### 11.3 v3：技能与世界演化

在稳定工作台和长篇一致性之后，再推进 Skill Mining、自进化、复杂世界运行态。
不要过早把系统做成“世界模拟研究平台”，否则容易牺牲产品可用性。这个结论也符合旧稿对风险的警惕。

---

## 12. 最终推荐结论

我给你的最终判断是：

### 应该保留的部分

旧稿里的这些必须保留：

* 创作引擎 + 编辑工作台双核定位
* 作者控制权最高
* 中篇优先
* 工件、编辑、版本系统优先
* 不做聊天壳
   

正式版里的这些必须保留：

* 多 Agent 流水线
* SceneSimulator / WriterAgent 分层
* 四大检索基座
* GroundingPack 思路
* ConsistencyJudge 与 RepairAgent
* 结构化 Schema 与 API 化意识
    

### 应该弱化的部分

* 过度强调“世界模拟为核心，文本只是渲染层”的表述
  因为对很多题材，尤其现实、爱情、悬疑、短中篇来说，世界模拟不是唯一中心，**Scene 规划 + 文本修订 + 版本决策**同样核心。正式版这个表述偏研究系统，不够产品化。

### 应该新增的部分

* ScenePlanner 作为正式 Agent
* RevisionTask / VersionRecord / EvaluationIssue 三件套
* 工作台页面级状态机
* UI 状态持久化
* 问题—修订—版本联动
* BootstrapAgent 与 ProjectCharter
  这些都是从你上传 HTML 和旧稿合并后自然得出的最强补充。  


