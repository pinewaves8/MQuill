我建议把这件事定义成“把版本页升级为版本工作台”，目标不是只补一个历史列表，而是把它变成未来 生成 → 评估 → 修订 → 版本 → 再评估 闭环的中枢。

现状判断
当前 repo 已经有 VersionRecord、版本比较页、版本抽屉、恢复接口，也在 revision-candidates/apply 时创建了前后两个版本快照。但它还不够支撑闭环，主要差三件事：

自动生成链路没有稳定落版本
现在大纲生成、章节导入、场景生成、正文生成能跑通，但“生成完成后形成可追溯版本”这一步没有统一治理。

版本还是“线性快照”，不是“可追踪演化节点”
虽然有 parentId、branchName，但 UI 和流程层面还没真正支持“主线/分支/来源/关联 issue / 关联修订”的演化视角。

版本页还没对齐 c_modified_v17.html
HTML 里的版本页其实已经定义了正确方向：版本卡片、来源摘要、分支、自动比较、手动保存、恢复、加入比较、与评估/修订联动。现在 repo 版本页还是一个简化版 compare 页面。

我建议的最合理方案
采用“章节级版本图谱 + 当前工作副本”的方案。

保留“当前编辑态”与“已保存版本”分离
当前正文继续是 draft segments，可随时编辑。
版本记录只在关键节点落快照，不把每次输入都当版本。

版本记录以“章节”为最小闭环单元
因为评估、修订、恢复、比较基本都围绕章节发生。
项目级版本页负责聚合和筛选，不直接取代章节级版本历史。

每个版本节点必须带上“来源元数据”
至少包含：
type、source、summary、parentId、branchName、linkedRevisionId、linkedIssueIds、trigger
这样后面才能回答：
“这个版本是谁生成的”
“它解决了哪个问题”
“它是从哪个版本分出来的”
“为什么变成现在这样”

明确三类版本
baseline
生成链路产生的阶段性基线版本，比如“自动生成正文初稿”
revision
针对问题或人工意图产生的修订结果版本
branch
不直接覆盖主线，只作为备选写法存在

主线只维护一个 current，但允许多分支并行
这最适合未来小说质量迭代：
主线代表当前采用稿
分支代表备选修订、不同风格尝试、局部重写方案

版本页对齐 c_modified_v17.html 的产品计划
Phase 1：补齐版本生成时机
目标：先让系统“不会丢版本”。

自动生成正文完成时，创建首个正文基线版本
命名建议：初稿 v1
来源：auto-draft

手动保存正文时，支持“保存当前为新版本”
来源：manual-save

应用修订时，保留现有“修订前/修订后”双快照，但规范命名和来源
例如：
修订前备份
修订后版本
来源统一为 revision:<id>

恢复版本时，自动创建“恢复前备份”
避免误回滚

这一步完成后，至少生成链路、修订链路、恢复链路都可追溯。

Phase 2：版本工作台 UI，对齐 HTML
目标：把当前 /projects/[projectId]/versions 从比较页升级为工作台。

页面结构对齐 HTML
左侧：版本历史卡片流
右侧：当前章节版本比较区
下方或侧边：关联评估入口

版本卡片展示补齐
每张卡要显示：
版本名、类型、来源、时间、字数、摘要、分支名、关联问题

卡片动作补齐
恢复此版本
另存为分支版本
加入比较

比较区改为“切换即自动比较”
不是点按钮后才比较，而是左右版本一变就刷新 diff

增加“保存当前为新版本”入口
和 HTML 保持一致

编辑器侧的 VersionHistoryDrawer 也同步升级
不要只做一个简版抽屉，要跟主页面使用同一套版本卡视图模型

Phase 3：把评估与修订真正接到版本上
目标：形成质量提升闭环。

评估问题记录关联处理版本
当 issue 被送去修订并应用后，写回：
linkedVersionId
linkedVersionLabel
linkedVersionSummary

版本详情能反向看到“解决了哪些 issue”
这样版本不只是文本快照，而是“质量动作结果”

修订支持两种落地方式
apply to current
直接成为主线版本
save as branch
仅生成分支版本，不覆盖当前稿

版本页支持从 issue 反查版本、从版本反查 issue
这一步是闭环真正成型的关键

Phase 4：为未来持续优化预留的数据扩展
目标：现在不一次做完，但模型先设计对。

建议给 VersionRecord 扩展这些字段：
trigger
如 auto_outline、auto_scene_gen、auto_draft、manual_save、revision_apply、restore
stage
如 outline、scene_plan、draft、revision
linkedRevisionId
linkedIssueIds
metricsSnapshot
保存当时评估分数快照
isBranchHead
便于后续分支管理

这样未来就能做：
“哪些修订让质量分提升最多”
“哪条分支更优”
“哪个自动生成版本最值得保留为基线”

实施顺序
我建议按这个顺序做，风险最低：

先补“自动生成正文后自动落版本”
再重做 /versions 页面，直接对齐 c_modified_v17.html
然后补“保存当前为版本 / 分支版本”
再把评估 issue 和版本建立双向关联
最后补版本图谱和评估指标沉淀
我建议本次开发的边界
这一轮先做到“能用且方向正确”，不要一口气做成完整 Git 式版本树系统。最合适的 MVP 是：

自动生成正文后自动创建基线版本
/versions 页面对齐 c_modified_v17.html
支持手动保存、恢复、分支、自动比较
修订应用结果和评估问题建立关联
做到这四点，系统就已经具备闭环雏形，后面再往“多分支质量演化”扩展会很顺。

如果你认可这个方向，我下一步可以直接把它拆成一个可执行的开发任务清单，细到“先改哪些类型、API、页面、交互和存储字段”。




09:11
直接把它拆成一个可执行的开发任务清单，细到“先改哪些类型、API、页面、交互和存储字段”。
09:17



目标
把版本系统从“可比较的历史列表”升级成“版本工作台”，先支撑这条闭环：

自动生成正文 → 落基线版本 → 评估 → 发起修订 → 应用/分支 → 形成新版本 → 回看质量变化

下面按“先改什么、改哪些文件、每步产出什么”拆成可执行任务清单。

Phase 0：先定版本模型
目标：先把数据语义定稳，避免 UI 做完又返工。

扩展 VersionRecord 类型
文件：
packages/shared-types/version.ts
要改：
新增字段

trigger?: 'auto_outline' | 'auto_scene_plan' | 'auto_draft' | 'manual_save' | 'revision_apply' | 'restore' | 'branch_create'
stage?: 'outline' | 'scene_plan' | 'draft' | 'revision'
linkedRevisionId?: string
linkedIssueIds?: string[]
metricsSnapshot?: Record<string, number>
isBranchHead?: boolean
同时建议把 VersionType 调整为更贴近产品语义：

保留：manual revision branch
新增：baseline
可保留兼容：autosave current
如果怕改动面太大，这轮先不删旧值，只新增 baseline。
产出：

统一版本节点语义
后端和页面可以围绕同一结构开发
更新版本校验 schema
文件：
apps/web/src/lib/validation/schemas.ts
要改：

CreateVersionSchema
CompareVersionsSchema 保持
如新增手动保存/分支接口，补对应 schema
Phase 1：补齐存储层能力
目标：让 store 真能支持“基线、分支、关联修订/问题、项目级聚合”。

扩展版本存储接口
文件：
apps/web/src/lib/db/projects-store.ts
要改：
VersionStore 新增方法

getByProject(projectId: string)
getCurrentByChapter(chapterId: string)
createSnapshot(input)
createBranchFromVersion(versionId, options)
linkIssues(versionId, issueIds)
getVersionLineage(versionId) 先做简单 parent 向上追溯即可
同时规范 create() 行为：

新建 isCurrent: true 时自动取消同章其他 current
branch 默认 isCurrent: false
restore 后生成“恢复前备份”再切 current
持久化字段兼容
文件：
apps/web/src/lib/db/file-storage.ts
要改：

确认 versions 集合能无痛兼容新增字段
如果需要，给老数据补默认值逻辑
产出：

后面 API 和 UI 不需要自己拼业务规则
Phase 2：补齐版本创建时机
目标：先保证版本不会丢。

自动生成正文完成后落“基线版本”
重点文件：
apps/web/src/app/api/scenes/[sceneId]/generate-draft/route.ts
apps/web/src/app/projects/[projectId]/editor/page.tsx
要做判断：
当前是“按场景生成正文”，还是“整章正文生成”触发点更合理。
建议以“章节正文首次形成可读内容”作为落版本时机，而不是每个场景都落。

更合适做法：
在章节 draft 保存链路里判断：

该章无历史版本
且本次正文长度超过最小阈值
则创建：
type: 'baseline'
trigger: 'auto_draft'
stage: 'draft'
label: '初稿 v1'
summary: '自动生成正文初稿'
修订应用时规范版本写入
文件：
apps/web/src/app/api/revision-candidates/[candidateId]/apply/route.ts
要改：
当前已经会写“修订前/修订后”，但要补齐字段：

前快照：type: 'revision', trigger: 'revision_apply', linkedRevisionId
后快照：同上，并写 summary、parentId
branch 模式不要再 stub，要真正创建分支版本，不覆盖 current
恢复版本时规范写入
文件：
apps/web/src/app/api/versions/[versionId]/route.ts
要改：

恢复前创建 restore-backup
恢复后的 current 版本带 trigger: 'restore'
parentId 指向被恢复版本或当前版本，规则要固定
手动保存为新版本
新接口建议：
apps/web/src/app/api/chapters/[chapterId]/versions/snapshot/route.ts
请求体：

label
summary
source
可选 metricsSnapshot
行为：

读取当前 draft segments
创建一个 manual 版本
不改变 current 内容，只新增历史节点
从已有版本创建分支
新接口建议：
apps/web/src/app/api/versions/[versionId]/branch/route.ts
请求体：

branchName
summary
行为：

基于目标版本创建 branch 节点
parentId = versionId
isCurrent = false
isBranchHead = true
Phase 3：补齐项目级版本 API
目标：版本页不能只看“第一个章节”。

新增项目级版本聚合接口
新文件建议：
apps/web/src/app/api/projects/[projectId]/versions/route.ts
返回：

chapters
versions
可选 currentChapterId
支持 query：

chapterId
type
branchOnly
丰富章节版本接口
文件：
apps/web/src/app/api/chapters/[chapterId]/versions/route.ts
要改：
返回内容补充排序和衍生信息：

当前版本优先
其余按时间倒序
可附加 lineageDepth 或 isBranchHead
比较接口升级
文件：
apps/web/src/app/api/versions/route.ts
要改：

兼容 leftVersionId / rightVersionId 命名
返回结构可升级为：
leftVersion
rightVersion
diff
sentenceDiff
wordDiff
如果这轮不重写 diff 算法，先保留当前简单 diff，但把 response shape 先定出来
版本详情接口补信息
文件：
apps/web/src/app/api/versions/[versionId]/route.ts
要改：
GET 时可额外返回

lineage
linkedIssues
linkedRevision
这轮可以先只返回空或简单数据，先把 contract 立住
Phase 4：评估与版本打通
目标：版本能解释“它解决了什么问题”。

issue 写回关联版本
重点文件：
apps/web/src/app/api/issues/[issueId]/route.ts
apps/web/src/app/api/issues/[issueId]/create-revision/route.ts
apps/web/src/app/api/revision-candidates/[candidateId]/apply/route.ts
要改：
当 issue 触发 revision 且最终应用后：

回写 linkedVersionId
linkedVersionLabel
linkedVersionSummary
status 变成 fixed 或 in_revision
如果以分支方式应用，可记为 branch
修订任务记录 issue 来源
如果 RevisionTask 类型已有空间，补：
sourceIssueId?: string
相关文件：
shared types 和对应 store/schema

评估分数快照写入版本
在评估完成或修订应用后，把当时分数写进 metricsSnapshot
这样未来可以做“这个版本比上一个版本提升了多少”
Phase 5：版本页面重做，对齐 c_modified_v17.html
目标：让 /projects/[projectId]/versions 成为真正工作台。

页面改成“章节驱动”
文件：
apps/web/src/app/projects/[projectId]/versions/page.tsx
要改：

不再只读取第一个 chapter
页面顶部加章节选择器
选章节后刷新版本历史和比较区
页面布局对齐 HTML
建议拆组件：
components/version/version-workbench.tsx
components/version/version-history-list.tsx
components/version/version-card.tsx
components/version/version-compare-panel.tsx
components/version/save-version-modal.tsx
components/version/create-branch-modal.tsx
布局：

顶部：标题 + “保存当前为新版本” + diff 模式切换
左侧：版本历史卡片列表
右侧：左右版本选择 + 自动比较
卡片上提供动作：恢复、分支、加入比较
卡片字段对齐 HTML
每张卡显示：
label
type
source
summary
createdAt
wordCount
branchName
linked issue 标签
当前版本标识
比较区对齐 HTML
要求：
左右选择器切换即自动比较
支持“按句比较 / 按词比较”
高亮当前加入比较的卡片
保存当前为新版本交互
要做：
弹窗填写 summary
可选填写 source
提交到 snapshot 接口
分支交互
要做：
版本卡点击“另存为分支版本”
弹窗输入 branchName
创建成功后刷新列表
Phase 6：编辑器内抽屉同步升级
目标：编辑器里的版本体验不要落后于独立版本页。

升级版本抽屉
文件：
apps/web/src/components/version/version-history-drawer.tsx
要改：

复用 VersionCard
增加 加入比较
增加 创建分支
增加 查看关联问题
增加 保存当前为版本
编辑器页联动
文件：
apps/web/src/app/projects/[projectId]/editor/page.tsx
要改：

onSelectVersion 不能再空实现
可跳版本详情或在右侧预览
onRestoreVersion 后刷新 draft segments
从 evaluation 进入的“查看版本”可直接切到 versions tab 或打开版本页
Phase 7：补齐状态与前端 store
目标：让新页面可维护。

扩展版本 Zustand store
文件：
apps/web/src/lib/state/version-store.ts
建议状态：

selectedChapterId
selectedLeftVersionId
selectedRightVersionId
diffMode
filterType
showBranchOnly
如有必要增加页面本地 hook
建议新建：
apps/web/src/lib/hooks/use-version-workbench.ts
Phase 8：验证清单
每做完一段就验，避免最后一起炸。

类型验证
命令：
cd C:\tao\MQuill\apps\web && npx tsc --noEmit

关键流程手测
必须覆盖：

自动生成正文后出现首个基线版本
手动保存当前版本成功
修订应用后出现“修订前/修订后”版本
分支保存不覆盖 current
恢复版本后 current 正确切换
版本页左右切换能自动比较
issue 能看到关联版本
版本能看到关联 issue
建议开发顺序
这是最稳的落地顺序：

shared-types/version.ts + schema
projects-store.ts 版本能力补齐
revision-candidates/apply、versions/[id]/restore、新增 snapshot/branch API
自动正文生成后补基线版本
项目级 versions API
重做 /projects/[projectId]/versions/page.tsx
升级 version-history-drawer.tsx
打通 issue/revision/version 关联
全链路回归
我建议的本轮 MVP 边界
如果我们想先做一版能用的，先收这 6 项：

扩展 VersionRecord
自动生成正文后自动创建基线版本
新增“手动保存当前为新版本”
新增“从版本创建分支”
重做 /versions 页对齐 c_modified_v17.html
修订应用后把版本写回到 issue
这样就已经能形成第一版闭环。

如果你愿意，我下一步可以直接按这个清单开始做第一阶段，先从类型、store、API 下手。